spring:
  cloud:
    gateway:
      routes: []
      discovery:
        locator:
          enabled: false

  autoconfigure:
    exclude:
      - org.springframework.cloud.gateway.config.GatewayRedisAutoConfiguration

logging:
  level:
    com.platform: WARN
    org.springframework: WARN
