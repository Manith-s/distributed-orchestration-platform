name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven (skip tests for now)
      run: mvn clean install -DskipTests

    - name: Run Unit Tests Only (exclude integration tests)
      run: mvn test -Dgroups="!integration"
      continue-on-error: false

    - name: Upload Build Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: jar-files
        path: |
          **/target/*.jar
          !**/target/*-javadoc.jar
          !**/target/*-sources.jar

  code-quality:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: success()

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Code Quality Check
      run: mvn verify -DskipTests
      continue-on-error: true

  docker-build:
    runs-on: ubuntu-latest
    needs: [build-and-test, code-quality]
    if: github.ref == 'refs/heads/main' && success()

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build JARs
      run: mvn clean package -DskipTests

    - name: Build Docker Images
      run: |
        echo "Docker build would happen here in production"
        # Uncomment when Docker Hub credentials are configured
        # docker build -t ${{ secrets.DOCKER_USERNAME }}/orchestrator-service:latest orchestrator-service/
        # docker build -t ${{ secrets.DOCKER_USERNAME }}/worker-service:latest worker-service/
        # docker build -t ${{ secrets.DOCKER_USERNAME }}/query-service:latest query-service/
